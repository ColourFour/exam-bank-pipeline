{
  "verdict": "pass_with_risks",
  "acceptance_criteria_review": {
    "summary_field": "Pass. The audit report includes mark_scheme_text_foreign_label_summary.",
    "label_and_paper_buckets": "Pass. The summary includes by_foreign_label and top_papers buckets.",
    "ranked_review_candidates": "Pass. The report includes deterministic review candidates sorted toward records with more foreign labels and examples."
  },
  "test_quality_review": "The fixture asserts one contaminated record with known q7 and q9 foreign labels, plus controls for an unknown 90 label and a legitimate long q7 answer.",
  "repo_hygiene_review": "The change is confined to audit output shaping and test assertions.",
  "scope_creep_review": "No CLI, renderer, or generated-output behavior was changed in this iteration.",
  "regression_risks": [
    "Consumers that assume a fixed audit JSON shape should tolerate the added fields.",
    "Review candidates remain evidence for human triage, not proof that every candidate is semantically contaminated."
  ],
  "commands_run": [
    {
      "command": ".venv/bin/python -m pytest tests/test_audit.py -q",
      "result": "pass: 14 passed"
    },
    {
      "command": ".venv/bin/python -m exam_bank.cli output-integrity-audit --input output/json/question_bank.json --artifact-root output --output .agent-runs/2026-06-19T10-51-05-642Z/iteration-05/audit.current.integrity.json --example-limit 10",
      "result": "expected failure: current output still contains foreign mark-scheme text labels and suspicious rendered crop dimensions"
    }
  ],
  "evidence": [
    "The live audit report contains mark_scheme_text_foreign_label_summary with label buckets 2, 3, 4, 5, 6, 7, 8, 9, and 10.",
    "The live audit report contains 10 mark_scheme_text_foreign_label_review_candidates for the requested example limit.",
    "The top ranked live candidate is 63summer14_q07 with foreign labels 2, 3, 4, 5, and 6."
  ],
  "required_fixes": [],
  "recommended_next_planner_focus": "Use the ranked text-label candidates and suspicious-crop review pack to repair the highest-confidence stale mark-scheme artifacts."
}
