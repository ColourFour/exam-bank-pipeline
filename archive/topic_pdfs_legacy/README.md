## Topic PDF Archive

This directory preserves the old topic-PDF and review export code for reference only.

It is intentionally outside `src/exam_bank` and outside the active test suite because the supported product no longer includes:

- topic-PDF generation
- review CSV exports

If this code is revived later, expect to adapt it to the current extraction-only config and model surfaces before using it again.
