# Optional: wire prompt packets into your preferred agent CLI.
# Keep this tool-specific and local. Do not commit secrets.

# Example shape only:
# AGENT_EXEC='your-agent-cli --project . --prompt-file {packet}'
